"use client";

import { Toaster } from "react-hot-toast"

//Provider pour l'utilisation de toast sur l'application déclaré dans le layout principal.
export const ToasterProvider = () => {
    return <Toaster />
};